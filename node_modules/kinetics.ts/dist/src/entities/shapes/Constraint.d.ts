import { System } from "../../System";
import { EntityType } from "../../typings/Enums";
import { ConstraintInfo } from "../../typings/Interfaces";
import Entity from "../Entity";
/** A class which acts as a string constraint to an entity. */
export default class Constraint extends Entity {
    type: EntityType;
    /** The entity which the constraint is attached to. */
    entity: Entity;
    /** The length of the constraint, or the amplitude of the sinusodial wave. */
    length: number;
    /** The frequency of the sinusodial wave, in Hz. */
    frequency: number;
    constructor(info: ConstraintInfo, system: System);
    /** Renders the constraint. */
    render(context: CanvasRenderingContext2D): void;
    /** Updates the constraint. */
    update(): void;
}
//# sourceMappingURL=Constraint.d.ts.map