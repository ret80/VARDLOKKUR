import { System } from "../../System";
import { EntityType } from "../../typings/Enums";
import { LineInfo } from "../../typings/Interfaces";
import Vector from "../../utils/Vector";
import Entity from "../Entity";
/** A specific geometric entity which represents a straight line boundary. */
export default class Line extends Entity {
    type: EntityType;
    /** The vector which represents the starting point of the line. */
    get start(): Vector;
    /** The vector which represents the ending point of the line. */
    get end(): Vector;
    /** The vector which represents a vector from the start to the end. */
    get line(): Vector;
    /** The vector which represents the direction of the start -> end vector. */
    get lineDir(): Vector;
    /** The length of the line. */
    get length(): number;
    /** The thickness of the entity. */
    thickness: number;
    /** The inertia of the entity. */
    get inertia(): number;
    /** The system the line is in. */
    system: System;
    constructor(info: LineInfo, system: System);
}
//# sourceMappingURL=Line.d.ts.map