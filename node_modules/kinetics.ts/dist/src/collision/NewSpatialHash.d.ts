import { System } from "../System";
export default class NewSpatialHash {
    cells: {
        [key: number]: number[];
    };
    system: System;
    cellSize: number;
    queryId: number;
    constructor(system: System, cellSize: number, hashBucketSize: number);
    insert(x: number, y: number, w: number, h: number, id: number): void;
    query(): void;
    clear(): void;
}
//# sourceMappingURL=NewSpatialHash.d.ts.map