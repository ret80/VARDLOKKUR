# Очистка legacy и неиспользуемого кода VARDLOKKUR

Проект — 2D top-down RPG на PixiJS. После рефакторинга (task_01) накопился мёртвый код, дубликаты и временные файлы.

---

## 📁 Артефакты рефакторинга (task_01 пункт #2: GameState → GameStore)

**Важно:** `src/game/store/` — это НЕ legacy. Это активный артефакт рефакторинга по task_01 пункт #2 (разделение GameState на GameStore + Domain Models).

### Активные файлы (НЕ удалять):

| Файл | Статус | Описание |
|------|--------|----------|
| `src/game/store/game-store.ts` | ✅ Активен | GameStore — инкапсулированное хранилище состояния, замена GameState |
| `src/game/store/flag-domain.ts` | ✅ Активен | FlagDomain — типизированная модель флагов, используется GameStore |
| `src/game/store/world-entities.ts` | ✅ Активен | WorldEntities + коллекции — инкапсулированные коллекции сущностей |
| `src/game/store/models.ts` | ✅ WIP | Domain Model интерфейсы (PlayerModel, EnemyModel, DropModel, ProjectileModel) — экспортируются из `store/index.ts`, но пока не импортируются потребителями. Это план перехода на immutable модели |
| `src/game/store/index.ts` | ✅ Активен | Barrel export всех store-модулей |

### Merged в store (WIP, не интегрирован):

| Файл | Статус | Описание |
|------|--------|----------|
| `src/game/store/player-domain.ts` | ⚠️ Merged, 0 импортеров | Класс `PlayerDomain` с инкапсулированной логикой HP/damage/heal. Часть task_01 #2, но никогда не был подключён. **Можно удалить** |

---

## 🔴 Критические: файлы, которые можно удалить БЕЗОПАСНО

### 1. `src/game/engine.ts.bak` — старая резервная копия Engine
**Статус:** Никогда не импортируется. Пред-eecs версия движка с инлайн-физикой/AI/боем.
**Почему появился:** Бэкап перед рефакторингом ECS.
**Безопасность удаления:** ✅ 100% — нигде не используется.
**Действие:** `rm src/game/engine.ts.bak`

---

### 2. `src/game/store/player-domain.ts` — PlayerDomain (100+ строк)
**Статус:** НЕ legacy. Часть рефакторинга task_01 пункт #2 (GameState → GameStore + Domain Models).
**Содержимое:** Класс `PlayerDomain` с инкапсулированной логикой HP/damage/heal/position.
**Почему не используется:** Создан как часть плана, но никогда не был интегрирован. Engine работает напрямую с raw `Player` объектом.
**Действие:** НЕ удалять. Включить в план интеграции (см. task_03).

---

### 3. `src/game/models.ts` — Domain Model интерфейсы (80 строк)
**Статус:** НЕ legacy. Часть рефакторинга task_01 пункт #2.
**Содержимое:** `PlayerModel`, `EnemyModel`, `DropModel`, `ProjectileModel` — immutable domain model интерфейсы.
**Почему не используется:** Экспортируются из `store/index.ts`, но потребители не подключены.
**Действие:** НЕ удалять. Включить в план интеграции (см. task_03).

---

### 4. `src/game/generators/index.ts` — barrel-экспорт генераторов
**Статус:** Никогда не импортируется.
**Содержимое:** Пустой barrel-файл с re-export всех генераторов.
**Почему появился:** Планировалось централизованное экспортирование генераторов, но `world.ts` импортирует из подфайлов напрямую (`./generators/types`, `./generators/utils` и т.д.).
**Безопасность удаления:** ✅ 100% — pure re-export, 0 импортеров.
**Действие:** `rm src/game/generators/index.ts`

---

### 4. `fix-dialogue.js` — временный скрипт-патч
**Статус:** Никогда не импортируется. Один раз выполнен вручную.
**Содержимое:** 168 строк regex-замен для миграции `dialogue-system.ts` с прямого доступа к flag-ам на domain-методы.
**Почему появился:** Быстрый патч для приведения dialogue-system к новым паттернам из task_01.
**Безопасность удаления:** ✅ 100% — скрипт уже применён (dialogue-system.ts уже использует новые паттерны).
**Действие:** `rm fix-dialogue.js`

---

## 🟡 Средние: временные файлы разработки

### 5. Скриншоты в корне проекта
**Файлы:**
- `screenshot.png`
- `screenshot2.png`
- `screenshot3.png`
- `screenshot_quest_arrow.png`
- `screenshot_task01_done_20260831_1351.png`

**Безопасность удаления:** ✅ 100% — dev-артефакты, не участвуют в билде.
**Действие:** `rm screenshot*.png`

---

### 6. Dev-артефакты Vite
**Файлы:**
- `.vite-dev.pid`
- `.vite-server.log`

**Безопасность удаления:** ✅ 100% — пересоздаются при каждом запуске dev-сервера.
**Действие:** `rm .vite-dev.pid .vite-server.log`

---

## 🔵 Низкий приоритет: мёртвые экспорты внутри активных файлов

### 7. `game-states.ts` — мёртвые экспорты внутри активного файла
**Файл:** `src/game/game-states.ts` — **активный**, импортируется 4 файлами.

| Экспорт | Статус | Используется | Причина |
|---------|--------|-------------|---------|
| `ProjectileRt` | ✅ ЖИВОЙ | `engine.ts`, `drops-system.ts`, `combat-system.ts` | Расширяет `Projectile` из `entities.ts` |
| `DropRt` | ✅ ЖИВОЙ | `engine.ts`, `drops-system.ts` | Расширяет `Drop` из `entities.ts` |
| `GameEvents` | ✅ ЖИВОЙ | `event-bus.ts` | Типизированное описание событий |
| `GameState` | ❌ МЕРТВЫЙ | Никто | Заменена на `GameStore` |
| `ChestRt` | ❌ МЕРТВЫЙ | Никто | Дублируется в `world-entities.ts` |
| `PedestalRt` | ❌ МЕРТВЫЙ | Никто | Дублируется в `world-entities.ts` |
| `ShrineRt` | ❌ МЕРТВЫЙ | Никто | Дублируется в `world-entities.ts` |
| `NpcRt` | ❌ МЕРТВЫЙ | Никто | Дублируется в `world-entities.ts` |
| `DoorRt` | ❌ МЕРТВЫЙ | Никто | Дублируется в `world-entities.ts` |
| `FloatText` | ❌ МЕРТВЫЙ | Никто | Дублируется в `game-store.ts` |

**Безопасность удаления мёртвых:** ✅ 100% — ни один мёртвый экспорт не импортируется.
**Действие:** Удалить 7 мёртвых экспортов из `game-states.ts`. Оставить `ProjectileRt`, `DropRt`, `GameEvents`.

---

### 8. `hud-system.ts` — мёртвые методы
**Файл:** `src/game/system/hud-system.ts`

| Метод | Статус | Причина |
|-------|--------|---------|
| `onStats()` | ❌ Никогда не вызывается | Callback `onStats` в engine просто эммитит событие, но метод `onStats()` не вызывается |
| `updateMinimap()` | ❌ Никогда не вызывается | Миникарта обновляется в `engine.ts:948`, а не в HudSystem |

**Безопасность удаления:** ✅ 100% — 0 вызовов извне.
**Действие:** Удалить 2 метода из `HudSystem`.

---

## 🟣 Низкий приоритет: неиспользуемые зависимости

### 9. package.json — пакеты, которые не импортируются в src/

| Пакет | Версия | Причина |
|-------|--------|---------|
| `@dnd-kit/core` | ^6.1.0 | Drag-and-drop не используется в PixiJS-канвасе |
| `@dnd-kit/sortable` | ^8.0.0 | То же |
| `@dnd-kit/utilities` | ^3.2.2 | То же |
| `recharts` | ^2.10.0 | Графики в игре отсутствуют |
| `date-fns` | ^2.30.0 | Время форматируется вручную через `fmtTime()` |
| `lucide-react` | ^0.294.0 | Используются кастомные SVG-иконки |
| `react-router-dom` | ^6.8.0 | Single-page app, роутинг отсутствует |
| `framer-motion` | ^11.16.1 | Используются CSS-анимации |
| `uuid` | ^9.0.1 | UUID-генерация не найдена в коде |
| `@supabase/supabase-js` | — | Не импортируется в src/ (возможно, запланирован для будущего) |

**Безопасность удаления:** ⚠️ 95% — требуется ручная проверка перед удалением (возможно, используются в dev-инструментах или будущих фичах).
**Действие:** `npm uninstall <package>` после подтверждения.

---

## 🟢 Дубликаты типов (требуют рефакторинга, не простого удаления)

### 10. Три копии одних и тех же типов
Типы `ChestRt`, `PedestalRt`, `ShrineRt`, `NpcRt`, `DoorRt` определены в:
- `src/game/game-states.ts` — **мёртвые** (см. пункт #7, можно удалить)
- `src/game/store/game-store.ts` — **активные** (переопределяют типы из `world-entities.ts` с добавлением `x`, `y`, `g`)
- `src/game/store/world-entities.ts` — **активные** (базовые определения)

**Рекомендация:** После удаления мёртвых копий из `game-states.ts` (пункт #7) останется 2 копии. Вынести в `src/game/store/types.ts` и импортировать оттуда — это задача для task_03.

---

## 📋 План выполнения (по шагам)

### Фаза 1: Безопасное удаление (автоматизируемо)
1. Удалить `src/game/engine.ts.bak`
2. Удалить `src/game/generators/index.ts`
3. Удалить `fix-dialogue.js`
4. Удалить скриншоты `screenshot*.png`
5. Удалить dev-артефакты `.vite-dev.pid`, `.vite-server.log`
6. Удалить мёртвые экспорты из `game-states.ts` (7 экспортов: GameState, ChestRt, PedestalRt, ShrineRt, NpcRt, DoorRt, FloatText)
7. Удалить мёртвые методы из `hud-system.ts` (2 метода)
8. **Запустить TypeScript-компиляцию** — убедиться, что ничего не сломалось

### Фаза 2: Удаление зависимостей (требует проверки)
10. Проверить, что `@dnd-kit/*`, `recharts`, `date-fns`, `lucide-react`, `react-router-dom`, `framer-motion`, `uuid`, `@supabase/supabase-js` действительно не используются
11. Удалить через `npm uninstall`
12. **Запустить TypeScript-компиляцию**

### Фаза 3: Консолидация дубликатов (требует рефакторинга)
13. Вынести общие типы (`ChestRt`, `PedestalRt`, `ShrineRt`, `NpcRt`, `DoorRt`, `FloatText`) в `src/game/store/types.ts`
14. Заменить дублирующиеся определения в `game-store.ts` и `world-entities.ts` на импорты
15. **Запустить TypeScript-компиляцию**

---

## 📊 Итоговая статистика

| Категория | Элементов | Строк кода (~) | Безопасность |
|-----------|-----------|----------------|--------------|
| Мёртвые файлы (удалить) | 3 | ~100 | ✅ 100% |
| Временные файлы | 7 | ~0 | ✅ 100% |
| Мёртвые экспорты | 9 | ~120 | ✅ 100% |
| Мёртвые зависимости | 10 | — | ⚠️ 95% |
| Дубликаты типов | 6 × 3 = 18 | ~180 | 🔧 Рефакторинг |
| Незавершённые артефакты | 2 | ~270 | 📋 task_03 |
| **Итого** | **49** | **~1770** | |

---

## ⚠️ Риски

1. **`game-states.ts`** — содержит живые экспорты (`ProjectileRt`, `DropRt`, `GameEvents`). Удалять ТОЛЬКО мёртвые.
2. **`@supabase/supabase-js`** — может использоваться в будущих фичах (мультиплеер, облачные сохранения). Проверить `.gitignore` и комментарии.
3. **`lucide-react`** — может использоваться в UI-оверлее (если есть HUD-компоненты на React). Проверить `index.html` и структуру проекта.

---

## 🔧 Незавершённые артефакты рефакторинга (НЕ удалять)

Следующие файлы являются частью task_01 пункт #2 (GameState → GameStore + Domain Models), но никогда не были интегрированы:

| Файл | Строк | Описание |
|------|-------|----------|
| `src/game/store/player-domain.ts` | 188 | `PlayerDomain` — инкапсулированная модель игрока с методами `takeDamage()`, `heal()`, `useHeart()` и т.д. |
| `src/game/models.ts` | 80 | `PlayerModel`, `EnemyModel`, `DropModel`, `ProjectileModel` — immutable domain model интерфейсы |

**План действий:** Создать `task_03.md` с планом интеграции этих модулей в проект.

---

## ✅ Критерии завершения

- [ ] Все файлы из Фазы 1 удалены
- [ ] `npx tsc --noEmit` проходит без ошибок
- [ ] `npm run build` (или `npm run dev`) работает корректно
- [ ] Файл `task_03.md` создан с планом интеграции `player-domain.ts` и `models.ts`
